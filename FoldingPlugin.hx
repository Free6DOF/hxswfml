import haxegui.utils.Color;import XmlEditor;//{{{ EditorPluginclass FoldingPlugin extends EditorPlugin {	public var folds : Array<String>;	public static function main() {	}	public override function run() {		console.log("Folding");		// this.editor.tf.text += "\nFolded\n";	}	public override function redraw() {		var tf = editor.tf;		var gutter = editor.gutter;		editor.gutterWidth = 50;		tf.x = 50;		var scrollpane = editor.scrollpane;		var start = tf.getLineOffset(tf.scrollV-1);		var end = tf.getLineOffset(tf.bottomScrollV-1) + tf.getLineLength(tf.bottomScrollV-1);		for(i in start...end) {			var line = tf.getLineText(i);			// trace(StringTools.htmlEscape(line));			var xml = Xml.parse(line);			// var circle = new flash.display.Sprite();			// var g = circle.graphics;			// g.beginFill(Color.WHITE);			// g.drawCircle(0,0,4);			// g.endFill();			// editor.scrollpane.content.addChild(circle);			// circle.x = 30;			// circle.y = 30*i;		}	}	/*	public function new() {	super.new();	}	*/}//}}}